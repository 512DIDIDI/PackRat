<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/backgroundColorWhite</item>
        <item name="colorPrimaryDark">@color/backgroundColorWhite</item>
        <item name="android:textColorPrimary">@color/textColorGray</item>
        <item name="android:navigationBarColor">@color/backgroundColorWhite</item>
        <item name="colorAccent">@color/textColorGray</item>
        <!--改变toolbar菜单图标颜色-->
        <item name="android:textColorSecondary">@color/textColorGray</item>
        <!--toolbar菜单popup弹出风格-->
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
    </style>

    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <!--设置popup背景色-->
        <item name="android:colorBackground">@color/white</item>
        <!--设置popup位置在toolbar下-->
        <item name="overlapAnchor">false</item>
    </style>

    <!-- 对话框去除黑底 -->
    <style name="custom_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否漂现在activity上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 去除黑色边框的关键设置项 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 屏幕背景是否变暗 -->
        <item name="android:backgroundDimAmount">0</item>
    </style>

</resources>
